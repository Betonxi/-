"""
集中配置模块 —— 所有路径、密钥、模型参数统一管理。
优先读取环境变量，缺失时使用默认值。
"""
import os
from pathlib import Path

# ---------------------------------------------------------------------------
# 项目根目录
# ---------------------------------------------------------------------------
ROOT = Path(__file__).resolve().parent

# ---------------------------------------------------------------------------
# LLM 配置（优先环境变量）
# ---------------------------------------------------------------------------
LLM_API_BASE = os.getenv("LLM_API_BASE", "https://aistudio.baidu.com/llm/lmapi/v3")
LLM_API_KEY = os.getenv("LLM_API_KEY", "7b97b5e65d1248169aab2d56f67d2b0fbcb146a2")
LLM_MODEL = os.getenv("LLM_MODEL", "ernie-5.1")
LLM_MAX_RETRIES = int(os.getenv("LLM_MAX_RETRIES", "3"))
LLM_TIMEOUT = int(os.getenv("LLM_TIMEOUT", "60"))

# ---------------------------------------------------------------------------
# 数据库路径
# ---------------------------------------------------------------------------
DB_PATH_V1 = ROOT / "task1_output" / "teddy_task1.sqlite"
DB_PATH_V2 = ROOT / "task1_output_v2" / "teddy_task1_v2.sqlite"

# ---------------------------------------------------------------------------
# 知识库路径
# ---------------------------------------------------------------------------
KB_JSON = ROOT / "task3_kb" / "kb.json"
KB_INDEX = ROOT / "task3_kb" / "kb.index.pkl"

# ---------------------------------------------------------------------------
# 输出目录
# ---------------------------------------------------------------------------
RESULT_DIR = ROOT / "result"
RESULT_DIR.mkdir(exist_ok=True)

# ---------------------------------------------------------------------------
# 涉及公司（用于快捷索引）
# ---------------------------------------------------------------------------
COMPANIES = {
    "华润三九": "000999",
    "金花股份": "600080",
}
